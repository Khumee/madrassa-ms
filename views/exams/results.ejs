<%- include('../partials/header', { title: __('Exam_Results') }) %>
<div class="container mt-4">
    <h2><%= __('Exam_Results') %></h2>
    <p>Generate individual report cards for students.</p>
    
    <table class="table table-bordered mt-3">
        <thead class="table-dark">
            <tr>
                <th><%= __('Student_ID') %></th>
                <th><%= __('Name') %></th>
                <th><%= __('Class') %></th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            <% if (typeof students !== 'undefined') { %>
                <% students.forEach(s => { %>
                    <tr>
                        <td><%= s.id %></td>
                        <td><%= s.name %></td>
                        <td><%= s.class_name %></td>
                        <td>
                            <a href="/exams/<%= exam_id %>/student/<%= s.id %>/report-card" class="btn btn-sm btn-primary"><%= __('View_Report_Card') %></a>
                        </td>
                    </tr>
                <% }) %>
            <% } else { %>
                <tr><td colspan="4" class="text-center">Please enter an exam ID to view students.</td></tr>
            <% } %>
        </tbody>
    </table>
</div>
<%- include('../partials/footer') %>
